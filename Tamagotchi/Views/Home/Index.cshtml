@using Tamagotchi.Models;

<h1>Welcome to Tamagotchi World</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no pets yet!</p>
}
@if (Model.Count != 0)
{
  @foreach (Pet pet in Model)
  {
    <li>@pet.GetName()</li>
  }
}


</ul>
<a href="/tamagotchis/new">Add a new pet.</a>
